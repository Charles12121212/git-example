
1、使用空参构造创建对象 初始化一个长度为16的字符数组 用于存储数据

2、当添加数据时  如果最小容量（已经存在的字符数量 + 本次添加的数量） 不超过容量，直接添加 不扩容

3、如果超过，扩容
        先计算原数组长度 * 2 + 2作为新容量
        再判断这个新容量是否满足本次添加所需要的最小容量
            如果满足，使用这个新容量
            如果不满足，使用本次最小容量

4、扩容
    创建新容量长度的数组 将旧数组中的元素拷贝到新数组